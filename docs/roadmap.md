# Roadmap — SirilCMS

Состояние на 2026-09-25. Приоритет — сверху вниз внутри фазы.

Легенда: `[ ]` — не начато, `[~]` — в работе, `[x]` — сделано.

---

## Модель развёртывания

| Сценарий | Решение | Статус |
|---|---|---|
| Один клиент, один сайт, свой сервер | Одна установка (`make -C infra up`) | Работает |
| Один клиент, несколько сайтов на одном сервере | **Мультисайт в одной установке**: одна админка, одна БД, сайт резолвится по домену | Не поддерживается (фаза 3) |
| Разные клиенты на независимых серверах | **Дистрибуция**: версионированные готовые образы, одна установка на клиента | Частично — образ собирается на сервере клиента (фаза 2) |

Изоляция разных клиентов обеспечивается **разными установками**, не кодом. Мультисайт рассчитан на сайты одного владельца: граница доверия внутри установки — `canScope` (`editor` видит только свой сайт).

Не поддерживается и не планируется: несколько независимых установок на одном сервере за общим reverse proxy.

---

## Фаза 1 — фундамент (1–2 недели)

Цель: безопасность, корректность и защита от регрессий до роста функциональности.

### Баги

- [x] **Email-уведомления о заявках никогда не отправлялись.**
  Причина: `apps/web/server/utils/notify.ts` читал `site.settings.smtpHost` через публичный Payload API без токена, а поля `settings.*` доступны на чтение только `owner`. Исправлено: отправка перенесена в `apps/admin/src/utils/notify.ts`, вызывается afterChange-хуком на `form-submissions` (`overrideAccess: true`).
- [ ] **`settings.analyticsId` нигде не используется на web** (и недоступен ему по той же причине). Либо подключить счётчик, либо убрать поле.
- [x] **`make -C infra new-site` вводил в заблуждение.** Раньше советовал поднять второй compose-проект на том же сервере — падает на конфликте портов (Caddy `80/443`, admin `3001`). Цель переписана: печатает, что новый сайт = новый сервер (сценарий не поддерживается, см. «Модель развёртывания»).
- [x] `env_file: ../.env` в `infra/docker-compose.prod.yml` был захардкожен в трёх местах (плюс пять раз в `Makefile`). Параметризовано одной переменной `ENV_FILE` (default `../.env`), можно переопределить: `make -C infra up ENV_FILE=../.env.staging`.

### Безопасность

- [ ] Первый пользователь в пустой БД создаётся без auth и без явного `role: owner` становится `editor` — в системе не остаётся владельца. Принудительно выставлять `owner` первому пользователю в `beforeChange`-хуке `users` (не полагаться на seed).
- [ ] `smtpPass` хранится в БД открытым текстом. Шифровать (ключ из env) или вынести в env/секреты.
- [ ] Интеграционный тест draft-фильтра: публичный API без токенов → утечка черновиков сейчас ничем не покрыта.

### Качество / CI

- [x] GitHub Actions CI: `test` + `build` (postgres-сервис, `pnpm -r build`).
- [ ] ESLint (единый конфиг на монорепо) + `lint` в CI.
- [ ] `typecheck` (`tsc --noEmit` / `nuxt typecheck`) во всех workspace + в CI.
- [ ] CI-проверка миграций: `migrate` на чистой БД + проверка, что схема не требует новой миграции (нет забытого `migrate:generate` после правки контрактов блоков).

---

## Фаза 2 — дистрибуция и продукт (1–2 месяца)

### Дистрибуция (разные клиенты — разные серверы)

Цель: установка и обновление у клиента без сборки на его сервере и без ручных шагов.

- [ ] **Один универсальный образ web.** `NUXT_PUBLIC_MEDIA_BASE` передаётся как build-arg (`infra/docker-compose.prod.yml`, сервис `web`) — домен клиента зашит в образ. Перевести в runtime-конфиг (`runtimeConfig.public`, переопределение через env при старте).
- [ ] Аудит остальных build-time значений в `infra/docker/*.Dockerfile` — всё клиентское должно задаваться через env при запуске.
- [ ] Публикация образов `admin`/`web` в GHCR из CI по git-тегу (`vX.Y.Z`), semver.
- [ ] Prod-compose для клиента на `image:` вместо `build:`; версия — одна переменная в `.env`.
- [ ] Процедура обновления: `docker compose pull && up -d` (миграции уже применяются сервисом `migrate` при старте) + бэкап БД перед обновлением. Описать в [runbooks/prod.md](runbooks/prod.md).
- [ ] `CHANGELOG.md` с явной пометкой breaking-изменений и ручных шагов миграции.
- [ ] Установщик/чеклист первой установки: генерация `PAYLOAD_SECRET`/`POSTGRES_PASSWORD`/`PURGE_TOKEN`, создание owner.

### Продукт для контент-менеджера

Цель: снять трение в ежедневной работе с сайтом.

- [ ] **Live preview черновиков** (Payload live preview → Nuxt preview-route с draft-токеном). Сейчас результат виден только после Publish.
- [ ] **Медиа в S3-совместимом хранилище** (`@payloadcms/storage-s3`) — опционально, по env; локальный volume остаётся дефолтом для простых установок.
- [ ] Уведомления о заявках: шаблон письма, несколько получателей, статус доставки в `form-submissions`.
- [ ] Превью-картинки вариантов блоков в админке (визуальный выбор вместо select).
- [ ] Новые блоки и темы (по запросам клиентов). Процесс — [developer.md §4–5](developer.md).
- [ ] Дублирование страниц/блоков, шаблоны страниц («новый лендинг из шаблона»).

---

## Фаза 3 — мультисайт и масштаб (квартал+)

### Мультисайт в одной установке (один клиент — несколько сайтов)

Схема данных уже почти готова: `sites` (уникальный `domain`), обязательная связь `site` у pages/posts/categories/forms/form-submissions, `site-content` — одна запись на сайт, `canScope` для `editor`. Не готов web-слой — он рассчитан на один сайт в БД.

- [ ] Резолв сайта по `Host` на web; все публичные запросы (`/api/page`, `/api/site`, блог, sitemap, submit формы) скоупятся по сайту.
- [ ] Уникальность `slug` у pages/posts/categories/forms — в пределах сайта (сейчас `unique: true` глобально: два сайта не смогут иметь страницу `home`). Требует миграции индексов.
- [ ] Уведомления о заявках — в контакты сайта формы (сейчас `notify.ts` берёт `sites?limit=1`).
- [ ] Кэш и purge — per-site (ключ кэша включает host; purge только затронутого сайта).
- [ ] Caddy: несколько доменов на одну установку (список доменов из env или on-demand TLS с проверкой домена через admin).
- [ ] Медиа: `NUXT_PUBLIC_MEDIA_BASE` не должен зависеть от домена сайта (общий admin-домен установки или относительные пути через Caddy).

### Масштаб и надёжность

- [ ] **i18n контента** (Payload localization): сейчас переведён только UI админки, лейблы полей блоков — русские строки.
- [ ] **E2E-тесты** (Playwright): создать страницу в админке → проверить на сайте → опубликовать → проверить purge кэша.
- [ ] **Наблюдаемость**: метрики cache hit/miss (хедер `x-siril-cache` уже есть), логи purge-хука (сейчас fire-and-forget — ошибки теряются), алерты на провал бэкапа.
- [ ] Версионирование контрактов блоков: процесс для breaking-изменений в `BLOCKS` (сейчас append-only по соглашению) — миграция данных + deprecation вариантов. Критично после появления дистрибуции: у клиентов разные версии и данные.
